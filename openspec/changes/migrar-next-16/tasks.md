## 0. Setup: rama de trabajo y línea base (OBLIGATORIO, PRIMER PASO)

- [x] 0.1 Crear la rama `feature/migrar-next-16` desde `main` actualizado y verificar con `git status -sb` que es la rama activa y está limpia
- [x] 0.2 Registrar la línea base antes de tocar nada (el agente lo ejecuta): guardar la salida de `npm audit --omit=dev` (debe coincidir con 10 alertas: 1 crítica, 1 alta, 8 moderadas) y ejecutar la matriz completa de `curl` de `design.md` (decisión 10) contra `npm run dev` sin `.env.local`; detener el servidor; verificar que el reporte `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-0-linea-base.md` existe con cada respuesta (código HTTP y cuerpo)

## 1. Bloque A — Next 16 y ESLint 9

- [x] 1.1 Instalar en una sola orden `npm install next@^16.3.5 && npm install -D eslint@^9 eslint-config-next@^16.3.5` (o los tres en un solo `npm install`, para evitar `ERESOLVE` entre `eslint@8` y `eslint-config-next@16`); verificar con `npm ls next eslint eslint-config-next react react-dom --depth=0` que instala 16.3.x, 9.x, 16.3.x y que `react`/`react-dom` siguen en 18.3.x, y con `npm ls postcss` que ya no existe `postcss@8.4.31` anidado bajo `next`
- [x] 1.2 Crear `eslint.config.mjs` con `eslint-config-next/core-web-vitals` y los `globalIgnores` de `design.md` (decisión 4), eliminar `.eslintrc.json` con `git rm` y cambiar el script `lint` de `package.json` a `eslint .`; verificar que `npx eslint --version` es 9.x y que `npm run lint` termina sin errores. Si una regla nueva de la 16 marca código existente, desactivarla en `eslint.config.mjs` con comentario que cite este cambio y anotar el hallazgo para `docs/ESTADO-PROYECTO.md` (no refactorizar); si marca `*.config.js`, añadir el bloque `sourceType: "commonjs"`
- [x] 1.3 `lib/adminAuth.ts`: reemplazar `cookies().get(COOKIE)` por `const store = await cookies(); store.get(COOKIE)`; verificar que `grep -rn "cookies()" lib app` solo devuelve la línea con `await` y que `npx tsc --noEmit` no reporta errores en ese archivo
- [x] 1.4 Cambiar `params` a asíncrono en `app/admin/(panel)/leads/[id]/page.tsx`, `app/api/admin/leads/[id]/route.ts`, `app/api/admin/leads/[id]/pdf/route.ts` y `app/api/admin/team/[id]/route.ts` (firma `{ params }: { params: Promise<{ id: string }> }` y `const { id } = await params`, usando `id` donde antes iba `params.id`); verificar que `npx tsc --noEmit` pasa y que `npx @next/codemod@canary next-async-request-api . --dry --print` no propone cambios en otros archivos
- [x] 1.5 `app/layout.tsx`: añadir `data-scroll-behavior="smooth"` al `<html>`; `next.config.js`: añadir `agentRules: false` (conservar `reactStrictMode` e `images.remotePatterns`); verificar levantando `npm run dev` que arranca con Turbopack, que `git status` no muestra cambios en `AGENTS.md` ni `CLAUDE.md` tras cargar `/` en el navegador, y revisar el diff que Next aplique automáticamente a `tsconfig.json` (conservar `include` con `.next/dev/types/**/*.ts` y `jsx: react-jsx`, cambio obligatorio que Next reaplica en cada arranque; restaurar el formato compacto original; revertir y anotar cualquier otro); detener el servidor
- [x] 1.6 Verificación intermedia del bloque A (el agente la ejecuta): `npm run lint`, `npx tsc --noEmit`, `npm test` y `npm run build` (Turbopack) en verde, más la matriz completa de `curl` contra `npm run dev` comparada línea a línea con el paso 0; detener el servidor; verificar que existe `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-1-next16-eslint9.md` con las salidas, la comparación y las decisiones tomadas en 1.2 y 1.5

## 2. Bloque B — firebase-admin 14

- [x] 2.1 Ejecutar `npm install firebase-admin@^14.4.0`; verificar con `npm ls firebase-admin @google-cloud/firestore @google-cloud/storage` que instala 14.4.x, 9.x y 8.x sin `ERESOLVE`, y que `lib/firebaseAdmin.ts` no requiere cambios (`npx tsc --noEmit` sin errores)
- [x] 2.2 Ejecutar `npm run build` y `npm audit --omit=dev`; verificar que el build pasa y que el audit no reporta alertas críticas, altas ni moderadas fuera del residuo documentado en `design.md` (decisión 8: `uuid`/`gaxios` bajo `@google-cloud/storage`, módulo opcional no usado, sin parche publicado); guardar la salida íntegra y `npm audit fix --dry-run` como evidencia
- [x] 2.3 Ejecutar los `curl` de los endpoints admin de la matriz (`/api/admin/session` POST y DELETE, `/api/admin/config`, `/api/admin/team`, `/api/admin/team/abc`, `/api/admin/leads/abc`, `/api/admin/leads/abc/pdf`, `/api/admin/upload`, `/api/admin/test-email`) contra `npm run dev`; detener el servidor; verificar que las respuestas son idénticas al paso 0 y que existe `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-2-firebase-admin14.md` con el audit en 0 y las versiones instaladas

- [x] 2.4 (TDD, diseño decisión 13) Crear `lib/privateKey.test.ts` con casos para `normalizePrivateKey`: vacío/indefinido → `undefined`; PEM canónico se conserva; `\n` literales; comillas dobles y simples envolventes; CRLF; una sola línea con espacios; espacios al inicio/fin; etiqueta PKCS1 (`RSA PRIVATE KEY`) respetada; texto que no es PEM se devuelve recortado. Cada variante válida debe pasar por `crypto.createPrivateKey` sin error; verificar que `npm test` falla antes de implementar (módulo inexistente)
- [x] 2.5 Implementar `lib/privateKey.ts` (función pura, sin `server-only`) y usarla en `lib/firebaseAdmin.ts`; envolver `cert()`/`initializeApp()` en `try/catch` que registra `[firebase-admin] no se pudo inicializar` sin exponer la clave y devuelve `null` (memorizado) para que `getDb()`/`getAdminAuth()` degraden como sin credenciales; añadir en `.env.local.example` la nota de que en Vercel el valor va sin comillas; verificar que `npm test` pasa (test nuevo + 11 existentes), `npm run lint`, `npx tsc --noEmit` y `npm run build`
- [x] 2.6 Reproducir y verificar con `next start` local: con una clave RSA falsa envuelta en comillas, antes del cambio `/` responde error de servidor y después responde 200 con los defaults (log `[firebase-admin]` o `UNAUTHENTICATED` capturado); con la clave falsa sin comillas sigue en 200; detener el servidor; registrar en `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-2b-clave-privada.md` con la tabla de formatos aceptados por firebase-admin 12 y 14

- [x] 2.7 (diseño decisión 14) Reproducir el `ERR_REQUIRE_ESM` del preview en local con `node --no-experimental-require-module node_modules/next/dist/bin/next start` sobre el build actual (`/` → 500, log "Failed to load external module … firebase-admin…/auth … ERR_REQUIRE_ESM"); cargar `firebase-admin/auth` de forma perezosa y protegida en `getAdminAuth()` (asíncrona, `import()` en `try/catch`, fallo memorizado, log `[firebase-admin]`) y actualizar `lib/adminAuth.ts` y `app/api/admin/session/route.ts` a `await getAdminAuth()`; verificar `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`, repetir el simulacro (`/` 200, `POST /api/admin/session` → `500 {"error":"Auth no configurado en el servidor."}` con el log) y la matriz de `curl`; detener el servidor; registrar en `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-2c-auth-esm.md`; anotar en `docs/ESTADO-PROYECTO.md` que Vercel debe correr Node 24.x (el dueño lo fija en Settings → Build and Deployment) y en `docs/project-profile.md` §2 la comprobación pendiente

## 3. Verificación obligatoria (OBLIGATORIO — el agente la ejecuta, nunca el usuario)

- [x] 3.1 (Paso N) Ejecutar `npm run lint`, `npx tsc --noEmit`, `npm test` y `npm run build`; verificar que los cuatro terminan sin errores y registrar las salidas y tiempos en `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-3-verificacion.md` (comparar con la línea base del cambio anterior: 11 tests, build en verde)
- [x] 3.2 (Paso N+1) Prueba manual de endpoints con `curl`: matriz completa de `design.md` (decisión 10, los 11 endpoints de `docs/api-spec.yml` más la página `/admin/leads/abc`) contra `npm run dev` sin `.env.local`; verificar cada código y cuerpo contra el paso 0; no se persiste nada, así que no hay datos que restaurar (dejar constancia); detener el servidor y comprobar que el puerto ya no responde; añadir la tabla al reporte del paso 3
- [x] 3.3 (Paso N+2) Prueba E2E con Playwright MCP contra `npm run dev` (el agente la ejecuta; si el servidor MCP no conecta, la tarea queda abierta y se retoma en una sesión con Playwright, sin sustituirla por `curl`). Recorrido en 1280 px y 375 px: (a) landing `/`: secciones visibles, menú "Nosotros" con sus dos enlaces, clic en el CTA del hero y en el logo verifican el scroll suave a `#servicios` y `#inicio`, formulario de contacto completo hasta el estado de éxito; (b) `/diagnostico`: cuestionario completo, índice de fricción, estimador opcional con un proceso, envío y botón "Descargar mi informe (PDF)" con respuesta `application/pdf`; (c) `/equipo` y `/mision-vision` renderizan; (d) `/admin/login` muestra el formulario, `/admin` abre con la sesión dev, `/admin/leads` lista vacía, `/admin/leads/abc` muestra "Firebase no está configurado.", `/admin/config` y `/admin/equipo` renderizan los editores y en `/admin/equipo` seleccionar una imagen local muestra la previsualización (sin Blob); (e) si el dueño ha creado `.env.local` con credenciales de Firebase (nunca por chat), ejecutar además el login real, crear un lead de prueba desde `/diagnostico`, ver su detalle, exportar el PDF, eliminarlo con la confirmación "eliminar" (esto restaura los datos), guardar y revertir un toggle del CMS y subir una foto a un integrante de prueba que luego se elimina; si no existe `.env.local`, marcar esos pasos como "pendiente de preview" (tarea 5.2). Verificar que existe `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-3-e2e.md` con pasos, capturas y estado de datos antes/después/restaurado

## 4. Documentación (OBLIGATORIO — paso N+3)

- [x] 4.1 Actualizar `docs/project-profile.md`: §2 filas Framework (`16.3 / 18.3`), Backend (`16.3`), Base de datos y Auth (`firebase-admin 14`), Otras librerías (quitar la nota de migración pendiente); §5 fila "Lint / formato" (ESLint 9 con `eslint.config.mjs` y `eslint-config-next/core-web-vitals`; `npm run lint` = `eslint .`) y la fecha de la última pasada de los comandos obligatorios; verificar con `grep -n -E "14\.2|firebase-admin 12|next lint" docs/project-profile.md` que no queda ninguna mención
- [x] 4.2 Propagar a los estándares y agentes por edición directa (design, decisión 9): `docs/base-standards.md:2`; `docs/frontend-standards.md:2,16,23` (Next 16.3, Turbopack, ESLint 9, nota "APIs de Next 16: consultar `node_modules/next/dist/docs/`"); `docs/backend-standards.md:2,18,20,56` (Next 16.3, firebase-admin 14, firma `{ params }: { params: Promise<{ id: string }> }` con `await params`, `await cookies()`, misma nota de docs); `openspec/config.yaml:11`; `ai-specs/agents/backend-developer.md:3` y `ai-specs/agents/frontend-developer.md:3`, seguido de `/sync-agent-symlinks` para re-copiar a `.claude/agents/`; verificar que `grep -rn -E "Next(\.js)? 14|14\.2|firebase-admin 12|eslint 8|next lint" docs/*.md openspec/config.yaml ai-specs/agents .claude/agents` no devuelve nada fuera de `docs/backlog/` y del histórico de `docs/ESTADO-PROYECTO.md`
- [x] 4.3 Actualizar `README.md:5,10` (Next.js 16) y `docs/ESTADO-PROYECTO.md`: fecha de última actualización, stack (línea 8), pendientes 6 y 7 marcados como hechos con el resumen (versiones finales, ESLint 9, `npm audit --omit=dev` en 0, decisiones `agentRules: false` y `data-scroll-behavior`, reglas de lint desactivadas si las hubo como nuevo pendiente), y la línea "Verificación obligatoria" del flujo de trabajo (lint = ESLint 9); marcar `docs/backlog/migrar-next-16.md` como "implementada (pendiente de archivar)"; verificar con `grep -n "migrar-next-16" docs/ESTADO-PROYECTO.md` y con `git diff --stat` que `.env.local.example`, `docs/api-spec.yml` y `docs/data-model.md` no cambian

## 5. Cierre

- [x] 5.1 Revisar `git status` y `git diff --stat`: solo `package.json`, `package-lock.json`, `eslint.config.mjs` (nuevo), `.eslintrc.json` (borrado), `next.config.js`, `app/layout.tsx`, `lib/adminAuth.ts`, las cuatro rutas con `[id]`, `tsconfig.json` (solo si Next lo editó y se aceptó en 1.5), los documentos de la sección 4 y los artefactos del cambio; verificar que no hay `.env*`, `.next/`, `*.tsbuildinfo` ni cambios en `AGENTS.md`/`CLAUDE.md`; añadir `.playwright-mcp/` (artefactos locales del navegador de Playwright MCP: snapshots, consola, descargas) a `.gitignore` junto a `.worktrees/` y comprobar que desaparece de `git status`
- [x] 5.2 Validación en el preview de Vercel (única prueba que requiere las credenciales reales del dueño; se hace tras `/commit`, antes de mezclar a `main`): el agente entrega la lista de comprobación (login en `/admin/login`, abrir un lead, exportar su PDF, crear un lead de prueba desde `/diagnostico` y eliminarlo con confirmación, comparar landing/diagnóstico/equipo con producción en móvil y escritorio) y registra el resultado que informe el dueño en `openspec/changes/migrar-next-16/reports/AAAA-MM-DD-paso-5-preview.md`; verificar que el reporte queda con PASS antes de proponer el merge
